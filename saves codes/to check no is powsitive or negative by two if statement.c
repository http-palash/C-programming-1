#include <stdio.h>
 int main()
 {
 	int n;
 	printf("Enter a no : ");
 	scanf("%d",&n);
 	if (n>0)
 	{
    	printf("%d is positive ",n);
    }
    
   	if (n<0)
 	{
    	printf("%d is negative ",n);
    }
  return 0;
}
